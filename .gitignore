# Dependencies
node_modules/

# Local environment variables (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Note: dist/ is intentionally tracked — the Netlify site is deployed from it.
